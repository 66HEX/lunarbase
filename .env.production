# ===========================================
# LUNARBASE PRODUCTION CONFIGURATION
# ===========================================
# This is an example production configuration for LunarBase
# Copy this file to .env and customize the values for your deployment

# ===========================================
# APPLICATION CONFIGURATION
# ===========================================

# TLS certificate and private key paths
# These should be absolute paths to your SSL certificate files
TLS_CERT_PATH=/etc/ssl/certs/your-domain.pem
TLS_KEY_PATH=/etc/ssl/private/your-domain-key.pem

# ===========================================
# DATABASE CONFIGURATION
# ===========================================
# SQLite database file path
DATABASE_URL=/var/lib/lunarbase/production.db

# SQLCipher encryption key - CHANGE THIS!
# Use a strong, random password for production
SQLCIPHER_KEY=CHANGE-THIS-TO-A-STRONG-RANDOM-PASSWORD-FOR-PRODUCTION

# ===========================================
# SECURITY CONFIGURATION
# ===========================================
# JWT secret key - CHANGE THIS!
JWT_SECRET=CHANGE-THIS-TO-A-STRONG-RANDOM-JWT-SECRET-FOR-PRODUCTION

# Password pepper - CHANGE THIS!
# This should be a long, random string that is kept secret and never changes
PASSWORD_PEPPER=CHANGE-THIS-TO-A-STRONG-RANDOM-PEPPER-FOR-PRODUCTION

# ===========================================
# ADMIN CONFIGURATION
# ===========================================
# Initial admin user (created on first startup)
LUNARBASE_ADMIN_EMAIL=admin@your-domain.com
LUNARBASE_ADMIN_USERNAME=admin
LUNARBASE_ADMIN_PASSWORD=CHANGE-THIS-TO-A-SECURE-ADMIN-PASSWORD

# ===========================================
# LOGGING CONFIGURATION
# ===========================================
# Set log level for production
RUST_LOG=info,lunarbase=info

# ===========================================
# DEPLOYMENT NOTES
# ===========================================
# Server configuration (host, port, TLS, redirects, security headers) is handled via CLI arguments:
# ./lunarbase serve --host 0.0.0.0 --port 443 --enable-redirect --redirect-port 80 \
#   --force-tls --hsts-max-age 31536000 --enable-compression \
#   --csp-policy "default-src 'self'; script-src 'self' 'unsafe-inline'" \
#   --x-frame-options DENY
#
# 1. Replace all placeholder values with your actual production values
# 2. Ensure TLS certificate files are properly configured and accessible
# 3. Set appropriate file permissions (600) for this .env file
# 4. Consider using a secrets management system for sensitive values
# 5. Regularly rotate JWT_SECRET and PASSWORD_PEPPER (requires user re-authentication)
# 6. Monitor logs and metrics for security and performance
# 7. Set up automated backups for your database
# 8. Configure firewall rules to allow only ports 80 and 443
# 9. Consider using a CDN for static assets
# 10. Set up monitoring and alerting for your application